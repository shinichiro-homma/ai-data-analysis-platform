プロジェクトの現状を確認します。

Agent ツールを使い、**model: haiku** のサブエージェントに以下の作業を委譲してください。

サブエージェントの設定:
- subagent_type: `general-purpose`
- model: `haiku`

サブエージェントへのプロンプト:

```
プロジェクトの現状を確認し、以下の情報を報告してください。

## 1. 開発進捗

`docs/plan/README.md` を Read ツールで読み、各カテゴリファイル（`docs/plan/01-jupyter.md` 等）も Read して、以下を報告してください。カテゴリファイルには進行中・未着手のタスクのみがあり、これだけで現状把握は完結します（`docs/plan/archive/` は完了済みのため通常は読まず、過去の経緯を問われた場合のみ参照）：

- 現在のフェーズ（進行中のフェーズ）
- 完了タスク数 / 全タスク数
- 進行中のタスク（ステータスが `[→]` のもの）
- 次に着手すべきタスク（ステータスが `[ ]` で、依存関係がないもの）

## 2. 進捗サマリー

Phase ごとにコンポーネント別の完了状況を以下の形式でまとめてください：

```
Phase X: {フェーズ名}
  - {コンポーネント}: X/Y 完了
```

## 3. ブロッカー

ステータスが `[!]` のタスクがあれば、その内容と理由を報告してください。
なければ「なし」と報告してください。
```

サブエージェントの結果をそのままユーザーに報告してください。

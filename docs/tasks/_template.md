# タスク詳細: {タスク番号} {タスク名}

## 概要

（このタスクで何を実現するか）

## 関連ドキュメント

- 要件定義: `docs/requirements/XXX.md` の「YYY」セクション
- API仕様: `docs/design/api-contracts.md` の「ZZZ」セクション

## 調査したファイル

- `path/to/file.ts`: （把握した要点を一言。調査エージェントの結果または自分の Read に基づく）

## 検討した代替案

| 案 | 概要 | 利点 | 欠点 |
|----|------|------|------|
| A（採用） | （概要） | （利点） | （欠点） |
| B | （概要） | （利点） | （欠点） |

採用理由: （A を選んだ決め手）

ADR 要否: （状態の所有権・コンポーネント間契約・方式選定を決める場合は `docs/adr/` に起票。不要ならその理由を1行）

## 参考にする既存実装

- コピー元: `path/to/existing.ts`（同種の実装は現在 N 個目。3個目以降なら共通化の手順を実装計画に含める — `docs/design/invariants.md` I8）

## 異常系・不変条件

- 該当する異常系（切断・再起動・並行・タイムアウト・不正入力）: （このタスクでの挙動と、受け入れ条件のどれで検証するか）
- 関係する不変条件（`docs/design/invariants.md` の番号）: （例: I3, I5 — 抵触しないことをどう担保するか。抵触が必要なら ADR 参照）

## 実装計画

### 作成・変更するファイル

| ファイル | 内容 |
|----------|------|
| `path/to/file.ts` | （説明） |

### 参照ファイル（実装時に Read するファイル）

| ファイル | 参照理由・注目箇所 |
|----------|-------------------|
| `path/to/existing.ts` | （例: 同種実装のパターン。L40-80 のハンドラ構成） |

※実装・テスト準備フェーズはこの表と「作成・変更するファイル」「テスト参照ポインタ」の範囲でのみ作業する（範囲外の Glob/Grep 探索は禁止）。情報が不足していたら探索せず、不足内容を報告して終了する。

### 実装手順

1. （手順1 — 末尾に「検証: 実行コマンドと期待結果」を書く）
2. （手順2 — 同上）

### 技術的な考慮事項

- （考慮すべき点）

## リスクと検知方法

- （壊れうる箇所・影響を受ける既存機能）: （どう検知するか — テスト・grep・実行確認）

## 完了条件

- [ ] （条件1 — 実行コマンドと期待結果で書く）
- [ ] （条件2 — 同上）
- [ ] （異常系の条件を1つ以上含める — `.claude/rules/task-design.md`）

## テスト計画

### 既存テストの被覆状況

（計画時に `scripts/test-map.sh {コンポーネント名}` を実行して確認する。関連する既存テストを列挙し、流用可否を判断する。既存テストで十分なら Red フェーズは新規作成不要とその旨を書く）

- `path/to/existing.test.ts`: （何をカバーしているか。流用 / 参考のみ）

### 新規テスト（テストケース仕様）

（1 行 1 テストケースで全列を埋める。テスト準備フェーズはこの表を転記してテストコードにする。新規テスト不要なら表を置かず、不要である理由を 1 行で書く）

| テスト名 | 前提（Given） | 操作（When） | 期待結果（Then） | 対応完了条件 |
|---------|--------------|-------------|----------------|-------------|
| （例: `test_create_kernel_returns_201`） | （初期状態・使用する fixture） | （実行する操作・入力） | （検証するアサーション） | （「完了条件」の番号） |

### テスト参照ポインタ（テスト準備フェーズはこの範囲のみ Read する）

| 参照 | パス:行範囲 | 用途 | 注意点 |
|------|------------|------|--------|
| 模倣する代表テスト | `path/to/similar.test.ts:40-80` | （テスト構成の雛形。1 件に絞る） | （このパターンを使う理由・制約。例: 「fetch は DELETE body 非送信のため axios 経由」） |
| fixture / ヘルパー | `tests/conftest.py:255-330` | （使用する fixture・ヘルパー名） | |
| 規約チートシート | `.claude/skills/testing-strategies/reference/XXX.md` | （フレームワーク規約） | |

---

## レビューステータス

- [ ] 計画レビュー完了
- [ ] 実装完了
- [ ] テスト完了

# タスク詳細: 8.4 document-server tests の ruff 適用

## 概要

document-server の tests/ 配下のテストファイルに ruff format と ruff check --fix を適用する。

## 関連ドキュメント

- 類似タスク: docs/tasks/infrastructure/8.3-ruff-document-server-src.md

## 実装計画

### 対象ファイル

tests/ 配下の全 Python ファイル（9 ファイル）

### 実装手順

1. `ruff format document-server/tests/` を実行
2. `ruff check --fix document-server/tests/` を実行
3. `ruff check document-server/tests/` で残存エラー確認
4. `scripts/test.sh document-server` でテスト通過確認

## 完了条件

- [ ] ruff format が適用されている
- [ ] ruff check でエラーがない
- [ ] 既存テストが全て通る

## レビューステータス

- [x] 計画レビュー完了
- [ ] 実装完了
- [ ] テスト完了
